name: xWine mz Patchs ARM64CE Termux (longjunyu2/wine-custom)

on:
  push:
    branches: [ bleeding-edge ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: Checkout longjunyu2/wine-custom (custom-wine-9.16)
        uses: actions/checkout@v4
        with:
          repository: longjunyu2/wine-custom
          ref: custom-wine-9.16
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sudo dpkg --add-architecture i386
          sudo apt update
          sudo apt install -y \
            build-essential gcc-multilib g++-multilib \
            flex bison unzip pkg-config gettext \
            gcc-mingw-w64 libz-mingw-w64-dev \
            clang lld llvm \
            libxml-libxml-perl libssl-dev \
            libfreetype-dev libpulse-dev libsdl2-dev libfreetype-dev:i386 libfontconfig-dev:i386 \
            libdbus-1-dev libfontconfig-dev \
            libgnutls28-dev libopenal-dev \
            libasound2-dev libgstreamer-plugins-base1.0-dev \
            libgstreamer1.0-dev libglib2.0-dev

      - name: Setup LLVM MinGW
        run: |
          sudo mkdir -p /opt
          cd /opt
          sudo wget -q https://github.com/bylaws/llvm-mingw/releases/download/20250920/llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64.tar.xz -O llvm-mingw.tar.xz
          sudo tar -xf llvm-mingw.tar.xz
          EXTRACTED_DIR=$(tar tf llvm-mingw.tar.xz | head -1 | cut -f1 -d'/')
          sudo mv "$EXTRACTED_DIR" llvm-mingw
          sudo rm llvm-mingw.tar.xz
          echo "/opt/llvm-mingw/bin" >> $GITHUB_PATH

      - name: Setup NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r28c

      - name: Setup Termux sysroot
        run: |
          mkdir termux && cd termux
          wget https://github.com/xodiosx/Wine-arm64ce/releases/download/wine-14/termux-rootfs.tar
          tar xf termux-rootfs.tar
          sudo mv data /data
          sudo chown -R $(whoami):$(whoami) /data
          mkdir -p /data/data/com.termux/files/usr/tmp

      - name: Install libandroid-shmem and libandroid-spawn .deb packages
        run: |
          wget https://github.com/xodiosx/Wine-arm64ce/releases/download/1.1.0/libandroid-shmem_0.7_aarch64.deb
          wget https://github.com/xodiosx/Wine-arm64ce/releases/download/1.1.0/libandroid-spawn_0.3_aarch64.deb

          for deb in *.deb; do
            ar x "$deb"
            tar -xf data.tar.* -C /
            rm -f data.tar.* debian-binary control.tar.*
          done
          echo "Packages installed into /data/data/com.termux/files/usr"

      - name: Build Wine tools (host)
        run: |
          if [ ! -f ./configure ]; then
            ./autogen.sh
          fi
          mkdir wine-tools-build
          cd wine-tools-build
          ../configure --without-x --without-vulkan --without-gstreamer
          make -j$(nproc) __tooldeps__ nls/all
          cd ..
          mv wine-tools-build wine-tools

      - name: Build Wine ARM64EC (hybrid WoW64)
        run: |
          export WORKDIR="$GITHUB_WORKSPACE"
          export TOOLCHAIN="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/bin"
          export LLVM_MINGW_TOOLCHAIN="/opt/llvm-mingw/bin"
          export deps="/data/data/com.termux/files/usr"
          export SYSROOT="$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64/sysroot"
          export ARCH="aarch64"
          export SDKVER="28"
          export TARGET="aarch64-linux-android"
          export install_prefix="$WORKDIR/wine-build"
          export winetools_dir="./wine-tools"

          # ------------------------------------------------------------
          # 1. Android (Unix) compiler (NDK)
          # ------------------------------------------------------------
          export CC="$TOOLCHAIN/$TARGET$SDKVER-clang"
          export AS=$CC
          export CXX="$TOOLCHAIN/$TARGET$SDKVER-clang++"
          export AR="$TOOLCHAIN/llvm-ar"
          export LD="$TOOLCHAIN/ld"
          export RANLIB="$TOOLCHAIN/llvm-ranlib"
          export STRIP="$TOOLCHAIN/llvm-strip"

          # ------------------------------------------------------------
          # 2. Mingw (PE) compiler – make it first in PATH so Wine detects it
          # ------------------------------------------------------------
          export PATH="$LLVM_MINGW_TOOLCHAIN:$PATH"
          # Wine will find aarch64-w64-mingw32-clang and use it for PE.
          export DLLTOOL="$LLVM_MINGW_TOOLCHAIN/llvm-dlltool"

          # ------------------------------------------------------------
          # 3. pkg-config – fixed to use PKG_CONFIG_PATH (Wine's preference)
          # ------------------------------------------------------------
          export PKG_CONFIG_PATH="$deps/lib/pkgconfig:$deps/share/pkgconfig:$SYSROOT/usr/lib/pkgconfig"
          export PKG_CONFIG_LIBDIR="$PKG_CONFIG_PATH"   # fallback for older systems

          # ------------------------------------------------------------
          # 4. Compiler and linker flags
          # ------------------------------------------------------------
          export CPPFLAGS="-I$deps/include --sysroot=$SYSROOT"
          export CFLAGS="-Wno-declaration-after-statement -Wno-implicit-function-declaration -Wno-int-conversion"
          export LDFLAGS="-L$deps/lib -L$SYSROOT/usr/lib/aarch64-linux-android -Wl,-rpath-link=$deps/lib -Wl,-rpath-link=$SYSROOT/usr/lib/aarch64-linux-android"
          export LIBS="-landroid-shmem -landroid-spawn"

          export LIBRARY_PATH="$deps/lib:$SYSROOT/usr/lib/aarch64-linux-android"

          # Optional: Library hints (some configure scripts still need them)
          export FREETYPE_CFLAGS="-I$deps/include/freetype2"
          export PULSE_CFLAGS="-I$deps/include/pulse"
          export PULSE_LIBS="-L$deps/lib/pulseaudio -lpulse"
          export SDL2_CFLAGS="-I$deps/include/SDL2"
          export SDL2_LIBS="-L$deps/lib -lSDL2"

          # ------------------------------------------------------------
          # 5. Configure Wine
          # ------------------------------------------------------------
          ./configure \
            --enable-archs=aarch64,i386 \
            --prefix=${install_prefix} \
            --host=${TARGET}${SDKVER} \
            --with-mingw \
            --with-wine-tools="$winetools_dir" \
            --enable-win64 \
            --enable-nls \
            --with-pulse \
            --with-alsa \
            --disable-win16 \
            --disable-opencl \
            --disable-tests \
            --without-capi \
            --without-coreaudio \
            --without-cups \
            --without-dbus \
            --without-gphoto \
            --without-gssapi \
            --without-krb5 \
            --without-netapi \
            --without-oss \
            --without-pcap \
            --without-pcsclite \
            --without-sane \
            --without-udev \
            --without-unwind \
            --without-usb \
            --without-v4l2 \
            --without-wayland \
            --without-xinerama \
            --without-xxf86vm \
            --disable-amd_ags_x64 \
            --enable-wineandroid_drv=no

          make -j$(nproc)
          make install -j$(nproc)

          ls -la "$install_prefix/"

      - name: Strip binaries and clean up
        run: |
          cd wine-build
          rm -rf share/man share/doc
          cd ..

      - name: Package Wine
        run: |
          cd wine-build
          tar -cJf ../wine-termux-arm64ec.tar.xz .
          cd ..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: wine-arm64ec
          path: wine-termux-arm64ec.tar.xz

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: wine-${{ github.run_id }}
          name: Wine ARM64EC for Termux (longjunyu2/wine-custom 9.16)
          body: |
            **Wine hybrid WoW64 build for Termux (aarch64 + i386)**
            - Source: longjunyu2/wine-custom (custom-wine-9.16)
            - **Note:** This is a hybrid WoW64 build, not true ARM64EC (requires patched source).
            - Includes full gamepad patches (dinput, dinput8, xinput)
            - Extract to `/data/data/com.termux/files/usr`
          files: wine-termux-arm64ec.tar.xz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
